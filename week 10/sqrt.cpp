#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    int number;
    cout<<"Enter number: ";
    cin>>number;
    cout<<"the square root of "<<number<<" is "<<sqrt(number);
    return 0;
}